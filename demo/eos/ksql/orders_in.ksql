create or replace stream ORDERS (
  order_id bigint,
  user_id varchar,
  store_id varchar,
  quantity bigint,
  address struct<city varchar, state varchar, zipcode varchar>
) with (kafka_topic='orders_json', value_format='json');

describe ORDERS extended;
